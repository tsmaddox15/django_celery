[project]
name = "django-celery"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "celery>=5.6.3",
    "django>=6.1",
    "django-celery-results>=2.6.0",
    "flower>=2.1.0",
    "gunicorn>=26.2.0",
    "redis>=5",
]
